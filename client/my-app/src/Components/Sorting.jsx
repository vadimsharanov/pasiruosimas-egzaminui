function Sorting({ride,date}) {
    return (
        <div>
            <button onClick={ride} >Rida</button>
            <button onClick={date} >Data</button>
        </div>
    )
}
export default Sorting